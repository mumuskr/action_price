name: L2 Bear Flag
category: second_entry
direction: short
source:
  book: Trading Price Action Trends
  chapter: Unverified
  source_reference: Human verification required after local book ingestion
definition: Second downward attempt after a first attempt and renewed pullback in bearish context.
context:
  preferred:
    - bear_trend
    - strong_bear_trend
conditions:
  pullback_required: true
  second_attempt: true
entry:
  type: stop
  reference: signal_bar_low
stop:
  preferred: structural
target:
  configured_by_strategy: true
failure:
  - Price reaches the structural invalidation before the configured target.
notes:
  - Documentation only; the Python L1L2Detector is the computational proxy.
status: candidate
implementation: brooks_trader.patterns.l1_l2.L1L2Detector
